services:
  - type: web
    name: ai-email-app
    runtime: node
    buildCommand: npm install --build-from-source
    startCommand: node dist/index.cjs
    envVars:
      - key: NODE_ENV
        value: production
