services:
  - type: web
    name: ai-email-app
    runtime: node
    buildCommand: echo "No build needed"
    startCommand: node dist/index.cjs
    envVars:
      - key: NODE_ENV
        value: production
